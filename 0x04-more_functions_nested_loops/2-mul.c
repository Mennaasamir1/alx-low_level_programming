#include "main.h"

/**
 * mul - function that prints the multiplication of two numbers
 * @a: first integer
 * @b: second integer
 * Return: result
 */

int mul(int a, int b)
{
	int	result;

	result = a * b;

	return (result);
}
